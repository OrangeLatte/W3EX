# 项目文件导读

> 按阅读目的导览 w3ex 代码库。新人建议顺序：README → 本文档 → 《业务逻辑详解》→ 跑起来读垂直切片代码。

## 0. 仓库结构总览

```
w3ex/
├── README.md                    # 快速开始 + 定位
├── 业务逻辑详解.md               # 数据链路/执行层/评分规则逐层解释
├── 研究过程记录.md               # 决策与返工记录（为什么这么做）
├── 逻辑引用关系图.md             # 依赖方向 + 调用链 + 架构红线
├── 项目文件导读.md               # 本文档
├── docker-compose.yml           # postgres + redis + api + web
├── Dockerfile.backend / Dockerfile.frontend / .dockerignore
├── docs/                        # 设计契约文档（01-07）
├── backend/                     # Python FastAPI
└── frontend/                    # Next.js 15 Terminal UI
```

## 1. backend/（src-layout，包名 w3ex）

```
backend/
├── pyproject.toml               # 依赖 + ruff(线宽100, E/F/W/I/UP/B/SIM/N) + pytest(asyncio_mode=auto)
├── .env / .env.example          # W3EX_* 配置（真实源=rich，本机镜像覆盖见 .env.example 注释）
├── src/w3ex/
│   ├── main.py                  # FastAPI 入口：lifespan(init_db+空库seed) → 挂载 /api/v1 四组路由 + /health
│   ├── cli.py                   # python -m w3ex.cli init-db|seed|reset
│   ├── config.py                # Settings(BaseSettings, env_prefix=W3EX_)，全配置唯一来源
│   ├── core/
│   │   ├── schemas.py           # canonical Pydantic 结构（PriceQuote/Candle/Ticker/...）
│   │   ├── scoring.py           # regime 加权评分（btc5+eth3+sol2）
│   │   └── repository.py        # DB 查询封装（latest_price/price_change_pct/...）
│   ├── db/
│   │   ├── base.py              # Base/utcnow(UTC naive)/new_uuid/Mixin
│   │   ├── models.py            # 全部 ORM 表（含已退役的研究类表，见 docs/03）
│   │   ├── session.py           # async engine（sqlite→NullPool）
│   │   └── seed.py              # mock 种子数据（幂等，先 clear_all）
│   ├── providers/               # ★ 唯一外部数据出口
│   │   ├── base.py              # Provider 抽象基类 + ProviderUnavailable
│   │   ├── registry.py / factory.py  # 名称注册表 + env→实例组装
│   │   ├── http.py              # AsyncClient + TTLCache + 退避重试（基础设施）
│   │   ├── binance/             # market(klines/tickers/depth/...) + execution(depth_walk)
│   │   ├── coingecko/market.py  # 全市场 meta（市值/排名/global stats）
│   │   ├── jupiter/execution.py # DEX 真实 quote
│   │   ├── hyperliquid/execution.py
│   │   ├── composite/rich.py    # ★ 回退链编排（先读这里理解数据流）
│   │   └── mock/                # 降级兜底（确定性 seed）
│   ├── execution/router.py      # ★ 报价/确认/取消状态机（读这里理解交易闭环）
│   ├── intelligence/            # 业务组装层（market/assets/watchlist，纯 dict 组装）
│   └── api/
│       ├── deps.py              # FastAPI 依赖注入（DB 会话/Provider bundle）
│       └── routes/              # market.py assets.py trade.py watchlist.py（薄壳）
└── tests/                       # 27 个测试：API 形状/回退链/状态机/离线 Provider
```

### 建议精读顺序（后端）

1. `providers/composite/rich.py` —— 数据从哪来、怎么降级
2. `execution/router.py` —— 交易怎么报价、怎么确认
3. `api/routes/market.py` + `intelligence/market.py` —— overview 怎么聚合
4. `providers/http.py` —— 重试/缓存基础设施
5. `tests/test_providers.py` —— 回退语义的可执行规格

## 2. frontend/（Next.js 15 App Router）

```
frontend/
├── package.json / tsconfig.json / next.config.mjs / postcss.config.mjs
├── lib/
│   ├── api.ts                   # ★ 全部 TS 类型 + fetcher + ApiError（改契约先改这里）
│   └── format.ts                # fmtUsd/fmtNum/fmtPct/timeAgo 等展示格式化
├── components/
│   ├── ui.tsx                   # Panel/Badge/Delta/Bar/Sparkline/Skeleton/ErrorPanel
│   ├── sidebar.tsx              # 固定左栏导航
│   └── topbar.tsx               # 顶栏 + /health 在线状态轮询
└── app/
    ├── layout.tsx / globals.css # Tailwind v4 @theme 终端色板
    ├── page.tsx                 # 市场总览（Regime/Gainers/Losers/Volume/Funding/Liquidations）
    ├── markets/page.tsx         # 全量行情表（过滤+排序）
    ├── token/[symbol]/page.tsx  # ★ 垂直切片：价格/K线/盘口/funding/stats/Trade CTA
    ├── trade/page.tsx           # ★ 报价→单选→确认→PAPER 结果（Suspense 包 useSearchParams）
    └── watchlist/page.tsx       # 自选 CRUD + 实时价合并
```

### 建议精读顺序（前端）

1. `lib/api.ts` —— 类型契约即文档
2. `app/token/[symbol]/page.tsx` —— 行情展示的全部模式（含周期切换/盘口渲染）
3. `app/trade/page.tsx` —— 确认流程与状态展示

## 3. docs/ 设计契约（实现与文档冲突时，以代码为准并回改文档）

| 文件 | 内容 | 状态 |
|---|---|---|
| 01-product-boundaries.md | IN/OUT 边界（研究类模块已移除） | 已更新至 pivot 后 |
| 02-system-architecture.md | 模块图/数据管线/执行链路/部署 | 已更新 |
| 03-database-schema.md | 全部表 + 幂等键 + 「当前状态」节 | 已追加 pivot 说明 |
| 04-provider-interfaces.md | Provider 方法全集 + 回退策略 + env 表 | 已更新 |
| 05-api-contracts.md | API 响应形状（逐字段） | 已更新，与实测一致 |
| 07-mock-components.md | mock 定位=降级兜底 + 拓扑 | 已更新 |

（06-agent-workflow.md 已随研究模块移除而删除。）

## 4. 运行与验证命令速查

```bash
# 后端（必须 conda env orangeCai）
cd w3ex/backend && source /opt/anaconda3/etc/profile.d/conda.sh && conda activate orangeCai
pip install -e .[dev]
python -m w3ex.cli init-db && python -m w3ex.cli seed
uvicorn w3ex.main:app --port 8000        # 读 backend/.env（rich 真实源）

# 前端
cd w3ex/frontend && npm install && npm run dev   # :3000

# 质量门禁
ruff check --fix && ruff format && pytest -v      # 后端
npx tsc --noEmit && npm run lint && npm run build # 前端

# Docker 一键
docker compose up --build
```

## 5. 常见环境问题

| 症状 | 原因 | 处置 |
|---|---|---|
| 8000 端口返回陌生形状 | 陈旧进程占用 | `lsof -ti :8000 \| xargs kill -9` 后重启 |
| overview 里 funding 为空 | fapi 不可达 | 属预期降级；可设 `W3EX_BINANCE_FAPI_BASE` 指向可达镜像 |
| 价格/K线/盘口全 mock | sources 显示 mock | 检查网络或 `W3EX_BINANCE_SPOT_BASE` 是否指向可达镜像 |
| tsc 误报已删页面 | .next 残留 type stub | `rm -rf .next` 重跑 |
| liquidations 标注模拟 | 公开源无强平历史 | 属预期，UI 有标注 |
