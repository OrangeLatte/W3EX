# 业务逻辑详解

> w3ex — Web3 行情与交易终端（MVP）。本文按「数据从哪来 → 怎么加工 → 怎么呈现 → 怎么交易」的主线，逐层解释业务逻辑与关键实现决策。

## 0. 产品定位（一句话）

**不做托管型交易所，只做行情终端 + 多通道路由报价 + Paper 模拟执行。**

- 数据层：Binance（官方公开行情镜像 data-api.binance.vision）+ CoinGecko + 执行通道（Binance 盘口 / Jupiter DEX / Hyperliquid Perp），mock 仅作降级回退。
- 交易层：永不自主成交。所有订单由用户在 UI 明确点击 Confirm 后进入状态机，且 MVP 只做 paper fill。
- 研究类模块（AI Research / 叙事 / 钱包画像 / 链上信号）已在需求收敛中移除，数据库表保留但 API 不暴露（详见《研究过程记录》）。

## 1. 核心业务循环

```
Observe Markets  →  Compare Routes  →  Decide  →  Execute (Paper)
   看行情              比报价            确认       模拟成交
```

对应页面与 API 的映射：

| 循环阶段 | 页面 | 后端 API |
|---|---|---|
| Observe | `/` 市场总览、`/markets` 全量行情、`/token/[symbol]` 资产详情 | `/market/overview`、`/market/tickers`、`/market/klines/{symbol}`、`/market/depth/{symbol}`、`/assets/{symbol}` |
| Compare | 资产详情 → Trade CTA；`/trade` 报价表 | `/trade/quote` |
| Decide | 报价单选（best 高亮）+ 确认按钮 | `/trade/confirm` / `/trade/cancel` |
| Execute | 成交结果面板（PAPER SIMULATED 标识） | 同上 |

## 2. 数据链路：External API → Provider → Composite → 路由 → UI

```
data-api.binance.vision   api.coingecko.com   quote-api.jup.ag ...
        │                       │                    │
        ▼                       ▼                    ▼
  BinanceMarketProvider   CoinGeckoProvider   JupiterExecutionProvider ...
        └───────────────┬───────────┴────────────────────┘
                        ▼
      CompositeMarketProvider / CompositeExecutionProvider（rich）
        · 按方法组织优先级链，None/空结果 → 继续下一个，全失败 → mock 兜底
                        ▼
      api/routes（薄壳，Decimal 全部 str 化）
                        ▼
      前端 Terminal UI（响应里带 sources，UI 标注数据来源）
```

### 2.1 Provider 基础设施（providers/http.py）

所有真实数据源共享一个 HTTP 基础设施：

- 全局 `httpx.AsyncClient`（timeout 8s），进程内 TTLCache（按 cache_key + ttl 秒）。
- 429/5xx 指数退避重试（0.5s / 1s，默认 2 次），重试耗尽抛 `ProviderUnavailable`。
- 任何上游异常都不会击穿 API——在 Composite 层被捕获后降级。

### 2.2 Composite 回退链（providers/composite/rich.py）

`CompositeMarketProvider(primary=binance, meta=coingecko, fallback=mock)`：

| 方法 | 优先级链 | 说明 |
|---|---|---|
| get_tickers / get_global_stats / get_asset_meta | coingecko → binance → mock | CoinGecko 一次请求覆盖全市场市值/排名 |
| get_price / get_candles / get_depth / get_market_regime | binance → mock | K 线与盘口只信交易所 |
| get_funding / get_funding_summary | binance(fapi) → None | fapi 不可达时 funding 显示为空而非报错 |
| get_liquidations | 永远 fallback mock | 公开源无强平历史，UI 明确标注「模拟数据」 |

`CompositeExecutionProvider`：三个通道（binance / jupiter / hyperliquid）`asyncio.gather(return_exceptions=True)` 并发报价，收齐可用 route 后取 `total_cost_usd` 最低者为 best；三通道全部失败才落 mock 兜底（保证离线可演示）。

### 2.3 本机网络适配（关键决策）

开发机上 `api.binance.com` 不可达，但 Binance 官方公开行情镜像 **data-api.binance.vision** 可达。因此 Binance Provider 的 base URL 支持环境变量覆盖：

```
W3EX_BINANCE_SPOT_BASE=https://data-api.binance.vision/api/v3
W3EX_BINANCE_FAPI_BASE=...（fapi 不可达时 funding 自动回退 None）
```

执行 Provider（盘口吃单模拟）引用同一个 SPOT 变量，自动跟随镜像。

## 3. 市场总览的业务逻辑（GET /market/overview）

一次请求聚合出「此刻市场状态」，各部分来源：

1. **regime（市场体制）**：Binance 24hr ticker 的 BTC/ETH/SOL 24h 涨跌幅加权 `score = btc×5 + eth×3 + sol×2`（clamp ±100），映射 risk_on / risk_off / neutral + 中文 label + drivers（如 "BTC +34.5%"）。权重反映市值影响力排序。
2. **indices**：三大币 24h 变动（供 UI 顶部卡片）。
3. **gainers / losers / volume_leaders**：全量 tickers（去 USDT 后缀）排序后各取 10，字段统一 `{symbol, price, change_24h_pct, volume_24h_usd, high_24h, low_24h}`。
4. **global_stats**：CoinGecko /global（总市值、BTC 占比、24h 总量），可能为 null（限流时 UI 隐藏该卡）。
5. **funding**：Binance fapi premiumIndex，`rate` 为原始费率（8h 结算周期）。
6. **liquidations**：mock 模拟（来源标注 mock）。
7. **sources**：`{market, liquidations}` 让 UI 透明展示「这是真实价还是模拟数」。

## 4. 资产详情的业务逻辑（GET /assets/{symbol}）

一个 symbol 的完整行情切片：

- **price / change_24h_pct / change_1h_pct**：24hr ticker + 1h K 线差值（1h 可能 null）。
- **candles**：`interval ∈ {5m,15m,1h,4h,1d}`，`limit 10..500`，Binance klines 直转；UI 用 SVG Sparkline 渲染并可切换周期。
- **stats**：市值/排名/流通量等来自 CoinGecko `_markets` 缓存（TTL 60s）。
- **funding_rate**：永续资金费率（可 null）。
- **depth**：实时盘口 `{bids, asks}`，UI 画成量能条（bid 绿 / ask 红）；无盘口数据源时整体为 null，前端显示「无数据」而非报错。
- **sources**：`{market, meta}` 双来源标注。

## 5. 交易执行的业务逻辑（执行层三层模型）

```
用户意图 (side, asset, fiat_amount)
   → ExecutionRouter.get_quote：三通道并发报价
   → routes[]（含 price/滑点/费用/gas/预计到手/置信度），best_route_index = min(total_cost_usd)
   → 用户确认（UI 单选 route + 点击 Confirm）
   → 状态机 pending → executed | cancelled（过期/重复确认 422，不存在 404）
   → paper fill：filled = {side, asset_symbol, venue, price, receive, fees_usd, total_cost_usd}
```

### 5.1 三条通道的成本模型

| 通道 | 价格来源 | 滑点 | 费率 | gas |
|---|---|---|---|---|
| binance_cex | 盘口 depth_walk 逐档吃单 | 真实计算（吃单均价 vs 最优价） | taker 0.1% | 0 |
| jupiter_dex | /v6/quote 真实路由报价 | priceImpactPct×100 | 0.04% + gas≈$0.02 | 含 |
| hyperliquid_perp | /info allMids | 固定估计 0.05% | 0.045% | 0 |

**depth_walk 是唯一的「真实滑点」**：沿 Binance 盘口逐档累加数量直到吃满 `$amount`，均价即成交价；深度不足抛 `ProviderUnavailable`（比如在 `/trade` 买一个小币种时该通道自动缺席，routes 列表只剩可用通道）。

### 5.2 安全边界（不可妥协）

1. AI/系统**永不自主交易**——本产品已无自主 Agent，执行入口只有用户显式操作。
2. MVP 仅 paper fill：`trades.paper = true`，UI 结果面板固定显示 PAPER SIMULATED 徽章。
3. 报价有 `expires_at`，过期确认返回 422；每笔 quote 只能确认一次（防重放）。
4. 取消仅允许 pending 状态。

## 6. 自选列表（Watchlist）

- 单用户 MVP：`watchlists` 表按默认 user_id 存 symbol 列表。
- GET 返回数组；PUT 全量覆盖 `{symbols: [...]}`（前端 add/remove 都是改完整个列表再 PUT）。
- UI 在 `/watchlist` 页把列表与 `/market/tickers` 合并出实时价格/涨跌/成交量。

## 7. 数据持久化策略

- **行情类数据不落库**：价格/K 线/盘口全部实时拉取 + 进程内 TTL 缓存（60s 级），无定时任务、无历史积压——MVP 的正确取舍。
- **落库的只有交易意图**：trade_quotes（含完整 routes JSON 快照，可审计用户当时看到什么）、trades（成交结果）、watchlists。
- **研究类表保留**：entities/prices/onchain_events/news/narratives 等表仍在 ORM 与 seed 中，供未来回归研究模块，但 API 不暴露。

## 8. 评分与排序逻辑（保留的轻量智能）

- **Regime 评分**：见 §3.1，纯规则加权，透明可解释。
- **报价排序**：`total_cost_usd = price×qty + fees + gas`（buy 方向）直接比价，不做黑盒优化。
- **资金费率排序**：`|rate|` 降序，majors 优先展示。

## 9. 错误处理约定

- 统一错误体：`{"detail": {"code", "message"}}`；路由层薄壳把 KeyError→404、ValueError→422。
- 上游数据源不可用：降级链兜底，实在没有的数据在响应里是 `null` 或空数组 + sources 标注，**UI 区分「无数据」与「错误」**。
- 前端 `ApiError` 解析同一结构，页面级 ErrorPanel 展示。

## 10. 边界与已知局限

| 局限 | 说明 | 解除路径 |
|---|---|---|
| 单用户 | watchlist 无鉴权 | 加 auth 中间件 |
| Paper 执行 | 无真实下单 | 对接交易所下单 API（需 API key 管理） |
| funding 依赖 fapi | fapi 不可达时为空 | 增加备用永续数据源（Gate fapi 可达已验证） |
| liquidations 模拟 | 公开无强平历史 | 商用数据源（Coinglass 类） |
| 长尾币盘口 | 部分币无深度 | 已由 depth_walk 异常 + 通道缺席机制覆盖 |
