# 研究过程记录

> 记录 w3ex MVP 从立项到交付的关键决策、推演与返工过程。按时间序，重点是「为什么这么做」而不是「做了什么」（做了什么见《业务逻辑详解》与《项目文件导读》）。

## Phase 0 · 立项：为什么不是"真正的交易所"

初始设想是「AI-native Web3 Intelligence Exchange」（Bloomberg Terminal + On-chain Analytics + AI Research Agent + Execution Layer）。第一轮边界推演先排除了四类重资产：

1. **托管钱包/资金托管** —— 合规与安全成本远超 MVP 承载力；
2. **内部撮合引擎** —— 流动性冷启动是运营问题不是代码问题；
3. **清算账本** —— 同上；
4. **Neo4j 图数据库** —— PostgreSQL 关系表足够承载实体关系，图查询是未来优化不是前提。

第一版方向定为「看市场 → 找机会 → 分析资产 → 执行交易」的智能+执行终端，完成了 7 大模块（含 AI Research Agent、叙事智能、钱包画像、链上信号）与完整前后端（后端 31 个测试全绿、前端 7 页面三道门禁全绿）。

## Phase 1 · 需求收敛：一次关键 pivot

用户反馈：**"交易所不包含研究，只做行情和交易，数据源要丰富"、"完整"**。

这次收敛的本质是重新回答「产品的第一性价值是什么」：

- 研究类模块（Research/叙事/钱包/链上信号/新闻）的价值依赖**数据质量**——而免费链上数据源在开发环境不可达，mock 出来的"智能"没有可信度，反而稀释产品定位；
- 行情+交易是**不依赖稀缺数据也能做到真实可用**的闭环：Binance 公开行情镜像 + CoinGecko 就能支撑真实价格、真实盘口、真实 K 线；
- 「数据源要丰富」的正确解读不是堆数据种类，而是**每类数据都有多通道冗余 + 透明标注来源**。

决策：保留 Provider 抽象与数据库研究类表（回归成本低），砍掉 API 与 UI 暴露；全面接入真实数据源。

## Phase 2 · 真实数据源选型：网络可达性实测

开发环境逐个探测公开 API（curl 实测）：

| 数据源 | 结果 | 处置 |
|---|---|---|
| api.binance.com | 不可达 | — |
| data-api.binance.vision（Binance 官方行情镜像） | **200** | 选为主行情源，base URL 可 env 覆盖 |
| api.coingecko.com | 时通时不通（限流） | 作为 meta 源 + TTL 缓存 60s + 可空降级 |
| fapi.binance.com | 不可达 | funding 支持 env 覆盖，不可达时回退 None |
| quote-api.jup.ag / api.hyperliquid.xyz | 200 | 执行通道真实报价 |
| api.gateio.ws | 200 | 备用（已验证，未接入） |
| 强平历史类 | 均无免费公开端点 | 保留 mock + UI 标注「模拟数据」 |

**核心原则：任何上游失败都不能变成 UI 错误**——用 Composite 回退链把「数据缺失」降级为「该卡片显示无数据 / 标注来源」，而不是页面报错。

## Phase 3 · 执行层设计：滑点必须是算出来的

报价三通道里，最容易做假的是滑点。推演：

- 直接拿 mid price 加固定滑点 → 报价没有信息量，用户无法比较通道优劣；
- **Binance 通道用 depth_walk**：沿真实盘口逐档吃单直到吃满金额，均价 vs 最优价的差即真实市场冲击——这是唯一"算出来"的滑点；
- Jupiter 通道拿真实 priceImpactPct；Hyperliquid 只有 mid，诚实标注为估计值 0.05%。

三通道成本结构刻意差异化（CEX 有手续费无 gas / DEX 有 gas 无手续费溢价 / Perp 费率最低但滑点估计），使 best route 的比较真的有意义。

## Phase 4 · 安全边界推演：交易确认状态机

风险点：自动化系统与交易执行的组合。约束设计：

1. 系统中不存在自主决策的 Agent（Phase 1 已移除），执行入口唯一且必须由用户点击触发；
2. quote 带过期时间，`pending → executed | cancelled` 单向流转：过期确认 422、重复确认 422、不存在 404、取消仅 pending；
3. paper fill 强制 `paper=true`，UI 固定 PAPER SIMULATED 徽章——即使在 MVP 阶段也不给用户"已真实成交"的错觉；
4. quote 落库存完整 routes 快照，任何一笔"成交"都能审计到用户当时看到的价格与费用。

## Phase 5 · 工程过程记录

- **测试先行转测试同步**：Provider 层测试全部离线（monkeypatch `fetch_json`，网络零依赖），composite 回退用 _FlakyProvider 显式制造 `ProviderUnavailable`，验证「上游挂了 → mock 兜底」这条最关键的路径。27 个测试覆盖 API 形状、排序不变量、状态机边界。
- **契约漂移的教训**：冒烟阶段发现 8000 端口被陈旧进程占用，返回完全不同的 API 形状，差点误判为代码回退。处置：lsof 定位 + kill + 冒烟脚本固化全部契约断言（含页面 200 与已删模块必须 404），环境噪音从此可被一键识别。
- **前端门禁**：Next 15 + React 19 下 `useSearchParams` 必须包 Suspense 才能通过静态构建；删除页面后 `.next` 内旧 type stub 会让 tsc 误报，`rm -rf .next` 解决。
- **质量门禁固定序列**：后端 `ruff check --fix && ruff format && pytest -v`；前端 `tsc --noEmit / next lint / next build`；最后 node 冒烟脚本全绿才算交付。

## Phase 6 · 交付形态与未来回归点

最终 MVP = 纯行情终端 + 多通道路由报价 + Paper 执行，docker-compose 四服务部署（postgres/redis/api/web）。

为未来预留的回归点（表已在库、抽象已就位，只差 API/UI 层）：

- 研究模块回归：entities/prices/onchain_events/news/narratives 表 + seed 已在，Provider 接口可挂真实链上数据源；
- 真实执行：ExecutionProvider.execute 已是具体方法，接交易所下单 API 即可从 paper 切换；
- 多用户：watchlist 加 auth 即可；
- 备用行情源：Gate.io 已验证可达，注册进 registry 一行配置。
